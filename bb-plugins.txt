# bb plugins installed by setup.sh (install_bb_plugins). One source per line:
#   git:<url>[@ref]   npm:<pkg>[@ver]   path:<dir relative to this repo> (your own plugins)
# After installing/removing third-party plugins, refresh with:
#   bb plugin list --json | jq -r '.plugins[] | select(.provenance != "builtin" and .enabled) | .source' | grep -v '^path:'
# (path: lines are yours — the jq snapshot can't reproduce them, keep them by hand.)
git:https://github.com/yusuf8834/bb-sidebar.git@semver:^0.2.0
path:../bb-plugins/plugins/activity-overview
path:../bb-plugins/plugins/cmd-click-external
